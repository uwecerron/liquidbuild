{
  "name": "liquid-build-site",
  "version": "1.1.0",
  "private": true,
  "description": "Liquid Build website, CRM and MCP server (Vercel)",
  "scripts": {
    "dev": "node server.js",
    "build:pages": "python3 build.py",
    "indexnow": "node scripts/indexnow.js"
  },
  "dependencies": {
    "@vercel/blob": "^2.8.0",
    "postgres": "^3.4.5"
  },
  "engines": {
    "node": ">=18"
  },
  "devDependencies": {
    "esbuild": "^0.28.2"
  }
}
